# Retrobot 회고 — 2026-04-15 00:33 KST

## 요약

| 항목 | 값 |
|------|-----|
| 분석 기간 | 2026-04-14 23:56 ~ 2026-04-15 00:33 KST |
| 커밋 수 | 2 |
| 주요 변경 파일 | `app/infra/chroma.py`, `app/infra/bm25.py`, `app/infra/doc_store.py`, `app/infra/llm.py`, `app/api/query/service.py`, `app/api/document/service.py` |
| 에러 발생 | 0 (기록된 에러 없음) |

## 커밋별 활동

### `24a6bd3` — docs(retro): 자동 회고 생성 by Retrobot
- 이전 세션 회고 자동 생성

### `75242dd` — [BE] refactor: 안정성 및 품질 개선
- `app/infra/chroma.py`: `vector_search()`에 `min(n_results, count)` 클램핑 추가, count=0이면 빈 리스트 즉시 반환
- `app/infra/bm25.py`: `_rebuild()` 제거, ChromaDB I/O를 Lock 밖으로 분리해 검색 블로킹 해소
- `app/infra/doc_store.py`: `get()`, `list_all()`, `exists()`에 `with self._lock:` 추가
- `app/infra/llm.py`: JSON 파싱 실패 fallback `has_relevant_content=False`로 수정
- `app/api/query/service.py`: `sources = referenced`로 변경해 빈 source_indices 시 전체 청크 노출 제거
- `app/api/document/service.py`: `except Exception` 블록에 `ErrorCode.SERVER_ERROR.code` 포함 `logger.error()` 추가

## KPT 회고

### Keep (좋았던 것)

- **기능 추가 전 기반 안정성을 먼저 확보하는 판단**: 캐싱 레이어를 얹기 전에 기존 파이프라인의 결함을 먼저 수정했다. 잘못된 응답이 캐시에 저장되면 TTL 동안 오류가 반복된다는 점을 인지하고 순서를 직접 결정했다.

- **커스텀 예외 체계와의 일관성 유지**: 단순 로깅 추가에 그치지 않고 기존 `ErrorCode` 체계와 연결해 로그 구조를 통일했다. 기존 설계를 존중하는 방식으로 기능을 붙인 좋은 사례.

- **환각 억제를 데이터 흐름 관점에서 접근**: `sources = referenced if referenced else chunks` 코드가 동작은 했지만 LLM이 출처를 지정하지 못한 경우 전체 청크를 노출하는 의미적 오류임을 파악하고 수정했다.

### Problem (나빴던 것)

- **들여쓰기 오류 반복**: `answer_stream()` 구현에서 들여쓰기 오류가 여러 번 발생해 검증을 3회 반복했다. 매번 diff를 보고 나서야 발견했다.

### Try (개선해야 할 것)

- **에디터 들여쓰기 자동화**: `.editorconfig`에 `indent_size = 4`, `insert_final_newline = true` 설정으로 파일 끝 개행과 들여쓰기 오류를 사전 차단.

<!-- 직접 추가하세요: 작업 중 놓쳤다고 생각되는 부분이나 다음 스프린트 계획 -->

## 타임라인

| 시각 (KST) | 커밋 | 주요 활동 |
|------------|------|----------|
| 2026-04-14 23:56~ | | 코드 전체 리뷰, 구현 계획 수립 |
| 2026-04-15 00:00~ | `f4530d5` | SSE 스트리밍 엔드포인트 완성 |
| 2026-04-15 00:33 | `75242dd` | 안정성 버그 6건 수정 |
