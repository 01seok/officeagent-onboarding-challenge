# Retrobot 회고 — 2026-04-14 23:56 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 분석 기간 | 2026-04-13 15:47 ~ 2026-04-14 23:55 KST |
| 커밋 수 | 3 |
| 주요 변경 파일 | `app/infra/llm.py`, `app/api/query/service.py`, `app/api/query/router.py` |
| 에러 발생 | 0 (기록된 에러 없음) |

## 커밋별 활동

### `2efb990` — docs: Retrobot 회고 (2026-04-13 15:47)
- 직전 세션 회고 파일명 정정 (`2026-04-13_1100_retro.md` → `2026-04-13_1541_retro.md`)
- `retros/TIMELINE.md` 업데이트

### `f936d3d` — [BE] feat: 스트리밍 응답 구현 (2026-04-13 18:01)
- `app/infra/llm.py` : `generate_answer_stream()` 비동기 제너레이터 추가
- 스트리밍 전용 시스템 프롬프트 `_STREAM_SYSTEM_PROMPT` 분리
  - 기존 `_SYSTEM_PROMPT`: JSON 구조화 출력 강제 (배치 응답용)
  - 신규 `_STREAM_SYSTEM_PROMPT`: 자연스러운 문장 응답 (스트리밍용)
- `query()` async generator의 텍스트 블록을 즉시 yield, 빈 문자열 제외 처리

### `f4530d5` — [BE] feat: 스트리밍 엔드포인트 구현(SSE) (2026-04-14 23:55)
- `app/api/query/service.py` : `QueryService` ABC에 `answer_stream()` 추상 메서드 추가, `QueryServiceImpl` 구현
  - 동일한 하이브리드 검색 → LLM 스트리밍 토큰 pass-through 구조
  - 검색 결과 없으면 단일 안내 문구 yield 후 종료
- `app/api/query/router.py` : `POST /query/stream` SSE 엔드포인트 추가
  - `StreamingResponse` + `media_type="text/event-stream"` 적용
  - `data: {token}\n\n` SSE 표준 포맷 준수

## KPT 회고

### Keep (좋았던 것)

- **레이어 분리 일관성 유지**: 스트리밍 기능 추가 시에도 LLM 인프라(`llm.py`) → 서비스 레이어(`service.py`) → 라우터(`router.py`) 계층을 그대로 따랐다. 신규 기능이 기존 아키텍처를 어기지 않아 코드 가독성이 유지됐다.
- **프롬프트 목적별 분리**: JSON 응답용 프롬프트와 스트리밍용 자연어 프롬프트를 별도 상수로 분리했다. 같은 LLM 호출이라도 응답 형식이 다른 두 경로에서 혼선 없이 각자 최적화할 수 있는 구조를 만든 것이 좋았다.
- **ABC 추상화 활용**: `answer_stream()`을 추상 메서드로 먼저 선언하고 구현체에서 채운 패턴이 서비스 계층의 계약을 명확히 한다.

### Problem (나빴던 것)

- **스트리밍 응답에 출처 정보 없음**: `/query/stream`은 현재 텍스트 토큰만 흘려보낸다. 기존 `/query` 엔드포인트에는 `sources` 필드가 있지만, 스트리밍 응답에서는 검색에 사용된 청크 정보를 클라이언트가 알 수 없다.
- **파일 끝 개행 없음**: 세 커밋 모두 `\ No newline at end of file` 상태로 저장됐다. 코드 리뷰 diff 가독성이 저하되고, 일부 린터 경고를 유발할 수 있다.
- **세션 로그 공백**: `f936d3d`, `f4530d5` 커밋에 대응하는 Claude Code 세션 로그가 없다. 작업이 세션 외부(직접 에디터 편집)에서 이루어져 회고에서 프롬프트·도구 사용 내역을 추적할 수 없었다.

### Try (개선해야 할 것)

- **스트리밍 종료 이벤트로 출처 전달**: SSE 표준의 `event:` 필드를 활용하거나, 스트림 마지막에 `data: [DONE] {"sources": [...]}` 형태의 메타 이벤트를 추가해 클라이언트가 출처를 수신할 수 있게 개선한다.
- **파일 끝 개행 확보**: 에디터 설정(`insert_final_newline = true` in `.editorconfig`) 또는 pre-commit 훅으로 자동 보정한다.
- **캐싱 레이어 연동 검토**: 현재 스트리밍 경로는 캐시를 거치지 않는다. 동일 질문에 대한 스트리밍 요청도 캐시 히트 여부를 응답해야 한다면, `answer_stream()` 서비스 레이어에 캐시 조회 로직을 추가해야 한다.

<!-- 직접 추가하세요: 작업 중 놓쳤다고 생각되는 부분이나 다음 스프린트 계획 -->

## 타임라인

| 시각 | 커밋 | 주요 활동 |
|------|------|----------|
| 2026-04-13 15:47 | `2efb990` | 회고 파일명 정정 및 타임라인 업데이트 |
| 2026-04-13 18:01 | `f936d3d` | LLM 스트리밍 응답 구현 (`generate_answer_stream`) |
| 2026-04-14 23:55 | `f4530d5` | SSE 엔드포인트 `/query/stream` 구현 |
