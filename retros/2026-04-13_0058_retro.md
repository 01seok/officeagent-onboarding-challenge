# Retrobot 회고 — 2026-04-13 00:58 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 10 |
| 총 프롬프트 | ~20 |
| 에러 발생 | 0 (로그 기준 tool_result error 없음) |

## 세션별 활동

### 세션 `77adfa6c` (2026-04-12 08:31 ~ 15:56, ~7시간 25분) — 핵심 구현 세션

**프롬프트:**
1. ver1_claude.md, officeagent_challenge_approach.md 읽고 Step 1 커밋 메시지 추천 → Step 2 진행
2. README.md, CLAUDE.md 재검토 요청 — 요구사항 재확인
3. BM25 + 벡터 검색 하이브리드 설계 맥락 공유 (Java 경험 기반, RRF + CompletableFuture → Python 이식 의도)
4. RAG 설계가 최우선 평가 항목이라는 판단 공유
5. 수정 필수 부분은 Claude가 직접, 코드 구현은 직접 타이핑
6. 개인 파일(ver1_claude.md, officeagent_challenge_approach.md)이 원격 저장소에 올라간 것 발견 → 조치 요청
7. Step 2 구현 설명 요청 (직접 타이핑할 것)
8. agent 모드로 commit 진행 요청 → 이후 직접 타이핑 방식으로 전환

**주요 작업:**
- Dockerfile, docker-compose.yml, requirements.txt 인프라 수정
- git filter-repo로 개인 파일 히스토리 완전 제거 + force push
- 문서 업로드 API 구현 (도메인, 레포지터리, 서비스, 라우터, DI)
- BM25 인덱스 구현 (`app/infra/bm25.py`)
- 쿼리 도메인, 스키마 구성
- 하이브리드 검색 repository 구현 (ChromaDB + BM25 병렬, RRF)

**커밋:**
- `[BE] feat : 문서 업로드 API 구현`
- `[BE] feat : RAG 검색 기반 구성 (BM25 인덱스, 쿼리 도메인, 스키마)`
- `[BE] feat: 하이브리드 검색 구현(BM25 + 벡터 병렬 검색, RRF)`

### 세션 `02477c98`, `191cc066`, `687c95b9`, `7297ab38`, `81a913ae`, `ad03e402`, `d58ff82e`, `db4c7a9f`, `f46a4f15` — Retrobot 자동 실행 세션

각 커밋 후 post-commit 훅에 의해 자동 실행된 Retrobot 세션.
주요 도구 사용: Bash, Write, Read, Glob, Agent

---

## KPT 회고

### Keep (좋았던 것)

- **하이브리드 검색 설계 적용**: Java 프로젝트(BM25:벡터 = 3:7, RRF, CompletableFuture)에서 검증된 설계를 Python/asyncio로 이식하여 RAG 품질을 높인 점. 기존 경험을 언어에 관계없이 추상화된 설계 레벨에서 재현했다.
- **개인 파일 유출 즉시 대응**: `git filter-repo`를 활용해 히스토리에서 완전 제거 후 force push로 원격 저장소 정리. 단순 삭제 커밋으로 넘기지 않고 히스토리 자체를 정리한 점이 깔끔했다.
- **협업 방식 명확화**: 설명 중심 지원 + 직접 타이핑 선호를 세션 초반에 확정하여 이후 불필요한 agent 모드 시도 없이 진행.
- **커밋 단위 분리**: 인프라 수정, API 구현, RAG 구성, 하이브리드 검색을 별도 커밋으로 분리하여 이력 추적성을 확보.

### Problem (나빴던 것)

- **개인 파일 .gitignore 미등록**: ver1_claude.md, officeagent_challenge_approach.md가 초기부터 .gitignore에 없어 원격까지 push됨. 사전에 등록했다면 filter-repo 작업 불필요.
- **agent 모드 요청 후 번복**: 세션 중반에 "agent 모드로 commit 진행" 요청 후 직접 타이핑으로 재전환. 협업 방식에 대한 일관된 사전 합의가 있었다면 더 매끄러웠을 것.
- **Retrobot 세션 과다 실행**: 하루에 9개의 Retrobot 세션이 생성됨. 커밋마다 훅이 실행되는 구조라 커밋 수만큼 Retrobot이 돌았으며, 각 세션이 소규모 반복 작업으로 리소스 소비.
- **venv 환경 확인 과정 반복**: python, python3, .venv/bin/python 경로를 순차적으로 시도하는 시행착오가 있었음. 환경 사전 확인 절차가 없었다.

### Try (개선해야 할 것)

- **작업 전 .gitignore 정비**: 새 파일 생성/작업 시작 전 작업 디렉토리 내 개인/임시 파일을 .gitignore에 먼저 등록하는 루틴 도입.
- **협업 모드 세션 초반 명시**: "설명 중심 지원 + 직접 타이핑" 방식을 CLAUDE.md 또는 세션 시작 시 명확히 전달하여 agent 모드 시도를 차단.
- **Retrobot 실행 조건 조정**: 모든 커밋에 훅이 걸리는 구조 대신, `[BE]`로 시작하는 기능 커밋에만 실행하도록 훅 조건 추가를 검토.
- **Python 실행 환경 사전 확인**: 세션 시작 시 `which python`, venv 활성화 상태를 1회 확인하는 선행 단계 추가. <!-- 직접 추가하세요 -->

---

## 타임라인

| 시각 (KST) | 세션 | 주요 활동 |
|------------|------|----------|
| 04-12 17:35 | f46a4f15 | Retrobot 자동 실행 |
| 04-12 17:53 | ad03e402 | Retrobot 자동 실행 |
| 04-12 08:31 ~ 15:56 | 77adfa6c | 핵심 구현 세션 (문서 API, BM25, 하이브리드 검색) |
| 04-12 23:25 | 7297ab38 | Retrobot 자동 실행 |
| 04-12 23:29 | db4c7a9f | Retrobot 자동 실행 |
| 04-12 23:30 | 687c95b9 | Retrobot 자동 실행 |
| 04-12 23:50 | 191cc066 | Retrobot 자동 실행 |
| 04-12 23:59 | d58ff82e | Retrobot 자동 실행 |
| 04-13 00:23 | 02477c98 | Retrobot 자동 실행 |
| 04-13 00:57 ~ 00:58 | 81a913ae | 현재 세션 (Retrobot) |
