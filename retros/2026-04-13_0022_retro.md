# Retrobot 회고 — 2026-04-13 00:22 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 9 |
| 총 로그 항목 | 457+ (메인 세션 기준) |
| 에러 발생 | 15+ |
| 커밋 수 | 11 |

## 세션별 활동

### 세션 `77adfa6c` (2026-04-12 17:31 KST ~ 2026-04-13 00:19 KST) — 메인 구현 세션

**수행한 작업:**
1. 이전 세션 컨텍스트 이어받기 (context overflow 후 재시작)
2. 프로젝트 기본 뼈대 구성 (FastAPI, settings, Dockerfile, docker-compose)
3. 문서 처리 인프라 구현 (embedding.py, chroma.py, doc_store.py, parser.py, chunker.py)
4. BM25 인덱스 구현 (bm25.py)
5. 문서 업로드 API 구현 (domain, schema, repository, service, router, DI container)
6. RAG 쿼리 도메인 및 스키마 구성
7. git 히스토리 클린업 (개인 작업 문서 제거 via git filter-repo)
8. Retrobot 설정 및 디버깅 (allowedTools 포맷 수정, dangerously-skip-permissions 적용)

**사용한 도구:** Bash(45), Read(23), Write(19), Edit(10), Glob(2), ToolSearch(1)

**발생한 에러:**
- git filter-repo 미설치 → pip 설치 후 해결
- Python venv 활성화 문제 → 직접 python 경로로 우회
- `File has not been read yet` 에러 (TIMELINE.md Write 시도)

### 세션 `f46a4f15` (2026-04-12 17:33 KST ~ 17:35 KST) — Retrobot 시도 1

**수행한 작업:** Retrobot 첫 실행 시도 (레트로 파일 생성 미완)

### 세션 `ad03e402` (2026-04-12 17:51 KST ~ 17:53 KST) — Retrobot 시도 2

**발생한 에러:**
- parallel tool call Bash 에러 (cancelled)

### 세션 `7297ab38` ~ `191cc066` (2026-04-12 23:22 KST ~ 23:50 KST) — Retrobot 반복 시도 3~7

**수행한 작업:** 회고 생성 반복 시도 — 모두 retros/ 파일 저장 실패 (TIMELINE.md read-before-write 오류 등)

### 세션 `02477c98` (2026-04-13 00:21 KST ~ 현재) — 현재 세션

**수행한 작업:** 현재 KPT 회고 생성 중

---

## KPT 회고

### Keep (좋았던 것)

- **레이어드 아키텍처 일관성**: infra → domain → service → router 순서를 지켜 구현하여 의존성 흐름이 명확하게 유지됨. 복잡도가 높아질수록 이 구조가 빛을 발함
- **커밋 단위 적절**: 기능별 커밋(인프라, 업로드 API, RAG 구성)으로 분리되어 git 히스토리가 읽기 쉬움
- **BM25 + ChromaDB 하이브리드 전략**: 벡터 검색과 키워드 검색을 함께 사용하는 설계는 실용적이고 평가자에게 설계 고민을 어필하기 좋음
- **Retrobot 자동화 도입**: post-commit 훅으로 회고를 자동화한 것 자체는 좋은 시도였고 결국 동작하도록 수정까지 완료

### Problem (나빴던 것)

- **개인 문서 실수 커밋**: `ver1_claude.md`, `officeagent_challenge_approach.md` 같은 개인 전략 문서가 커밋되어 git filter-repo로 강제 히스토리 수정이 필요했음. 불필요한 force push와 히스토리 오염이 발생
- **Retrobot 설정 시행착오**: `allowedTools` 포맷 오류와 `dangerously-skip-permissions` 미적용으로 인해 5회 이상 Retrobot 실행이 실패했고, 각 시도마다 새 세션이 생성되어 로그가 분산됨
- **TIMELINE.md Read-before-Write 미준수**: 여러 Retrobot 세션에서 TIMELINE.md를 먼저 읽지 않고 Write 시도해서 반복 실패
- **컨텍스트 오버플로우**: 메인 세션이 너무 길어져 context window 한계에 달해 세션 이전이 필요했음. 초기 설계 논의에 시간을 과하게 소비한 것이 원인

### Try (개선해야 할 것)

- **`.gitignore` 먼저**: 구현 시작 전 개인 작업 문서(전략 노트, 컨텍스트 파일 등)를 `.gitignore`에 추가하는 것을 체크리스트 1번으로 만들 것
- **Retrobot 드라이런 검증**: 훅 설정 후 `git commit` 이전에 `claude -p "$(cat retrobot/SKILL.md)"` 수동 실행으로 동작 확인 후 커밋에 의존할 것
- **TIMELINE.md 파일 존재 여부 체크**: Write 전 항상 Read 시도 → 없으면 신규 생성, 있으면 수정하는 패턴 준수
- **세션 길이 관리**: 설계 논의와 구현을 별도 세션으로 분리하거나, 구현 시작 전 플랜 문서를 확정하여 컨텍스트 낭비를 줄일 것

<!-- 직접 추가하세요: 위 KPT 외에 느낀 점이 있으면 여기에 기록하세요 -->

---

## 타임라인

| 시각 (KST) | 세션 | 주요 활동 |
|------------|------|----------|
| 17:31 | 77adfa6c | 컨텍스트 이어받기, 프로젝트 뼈대 구성 시작 |
| 17:33 | f46a4f15 | Retrobot 첫 시도 |
| 17:51 | ad03e402 | Retrobot 시도 2 (에러) |
| 17:xx | 77adfa6c | 인프라 모듈 구현 (embedding, chroma, parser, chunker) |
| 18:xx | 77adfa6c | 문서 업로드 API 구현 |
| 19:xx | 77adfa6c | BM25 구현, RAG 쿼리 도메인 구성 |
| 22:xx | 77adfa6c | git 히스토리 클린업, Retrobot 디버깅 |
| 23:22 | 7297ab38 | Retrobot 시도 3 |
| 23:26 | db4c7a9f | Retrobot 시도 4 |
| 23:27 | 687c95b9 | Retrobot 시도 5 |
| 23:46 | 191cc066 | Retrobot 시도 7 |
| 23:56 | d58ff82e | Retrobot 시도 6 |
| 00:21+1 | 02477c98 | KPT 회고 최종 생성 (현재) |
