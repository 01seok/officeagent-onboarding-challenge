# Retrobot 회고 — 2026-04-12 23:56 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 8 |
| 총 프롬프트 | 14 (주 세션) |
| 에러 발생 | 0 |

## 세션별 활동

### 세션 `77adfa6c` (02:31 ~ 08:56 KST) — 주 개발 세션

**프롬프트:**
1. ver_claude.md / officeagent_challenge_approach.md 읽고 현재 상태에 맞는 커밋 메시지 추천, step2 이동
2. README.md / CLAUDE.md 요구사항 검토 후 플랜 리뷰
3. BM25 하이브리드 검색 엔진(벡터 검색 3:7 RRF 가중치) 설명 요청
4. 중요한 수정사항 직접 적용, 이후 코드는 사용자가 구현
5. 현재 변경사항에 대한 커밋 메시지 추천
6. 프로젝트 흐름 및 기능 설명 요청
7. 개인 문서(ver1_claude.md, officeagent_challenge_approach.md) 실수로 커밋된 것 처리
8. 짧은 확인 ("B")
9. Retrobot 권한 관련 리뷰 기능 동작 여부 확인
10. Step 2 작업 설명 요청
11. 코드 타이핑 가이드와 함께 설명 요청
12. 에이전트 모드로 구현 계획 실행, 커밋 포함 (푸시 없이)
13. 짧은 확인 ("ㅇㅇ")
14. Retrobot이 회고를 작성하는 것인지 코드 리뷰를 하는 것인지 확인

**사용 도구:** Bash, Edit, Glob, Read, Write

**발생한 에러:** 없음

---

### 세션 `f46a4f15` (02:33 ~ 02:35 KST) — Retrobot 자동 실행

**프롬프트:** 없음 (자동 세션)

**사용 도구:** Agent, Bash, Glob, Read, Write

**발생한 에러:** 없음

---

### 세션 `ad03e402` (02:51 ~ 02:53 KST) — Retrobot 자동 실행

**프롬프트:** 없음 (자동 세션)

**사용 도구:** Agent, Bash, Glob, Write

**발생한 에러:** 없음

---

### 세션 `7297ab38` (08:22 ~ 08:25 KST) — Retrobot 자동 실행

**프롬프트:** 없음 (자동 세션)

**사용 도구:** Agent, Bash, Glob, Write

**발생한 에러:** 없음

---

### 세션 `db4c7a9f` (08:26 ~ 08:29 KST) — Retrobot 자동 실행

**프롬프트:** 없음 (자동 세션)

**사용 도구:** Agent, Bash, Write

**발생한 에러:** 없음

---

### 세션 `687c95b9` (08:27 ~ 08:30 KST) — Retrobot 자동 실행

**프롬프트:** 없음 (자동 세션)

**사용 도구:** Agent, Bash, Edit, Glob, Read, Write

**발생한 에러:** 없음

---

### 세션 `191cc066` (08:46 ~ 08:50 KST) — Retrobot 자동 실행

**프롬프트:** 없음 (자동 세션)

**사용 도구:** Agent, Bash, Edit, Glob, Read, Write

**발생한 에러:** 없음

---

### 세션 `d58ff82e` (08:56 ~ 08:56 KST) — Retrobot 자동 실행

**프롬프트:** 없음 (자동 세션)

**사용 도구:** Agent, Bash

**발생한 에러:** 없음

---

## KPT 회고

### Keep (좋았던 것)
- **에러 없는 안정적인 작업**: 8개 세션, 18개 이상의 파일 조작 작업에서 에러가 단 0건. 안정적인 도구 활용과 신중한 코드 작성 패턴이 잘 유지됨.
- **점진적 구현 방식**: 설명 → 사용자 직접 구현 → 리뷰 → 커밋이라는 명확한 흐름을 유지. 사용자가 코드를 직접 타이핑함으로써 실습 효과 극대화.
- **BM25 + 벡터 하이브리드 검색 설계**: RRF 3:7 가중치를 적용한 고품질 검색 파이프라인 설계. 단순 벡터 검색 대비 정밀도와 재현율 균형을 고려한 접근.
- **개인 문서 커밋 문제 즉시 처리**: 실수로 커밋된 개인 파일(ver1_claude.md 등)을 발견 즉시 처리하여 리포지터리 오염 최소화.

### Problem (나빴던 것)
- **Retrobot 반복 실행 (7회)**: Retrobot이 단일 세션에서 완료되지 못하고 7번 재시도됨. 이는 권한 문제 또는 실행 실패로 인한 반복 트리거로 추정됨.
- **Retrobot 권한 혼선**: 사용자가 "권한이 없어서 회고가 안 되는 것 같다"고 언급. Retrobot의 `dangerously-skip-permissions` 설정 또는 `allowedTools` 설정이 불안정했던 것으로 보임 (f801894, 52ed5a9 커밋 참조).
- **Retrobot 목적 혼선**: 사용자가 Retrobot이 KPT 회고 생성인지 코드 리뷰인지 헷갈림. 스킬 문서 또는 훅 설명이 불명확했던 것으로 추정.
- **개인 파일 커밋 실수**: ver1_claude.md, officeagent_challenge_approach.md 등 개인 작업 문서가 실수로 커밋됨. `.gitignore` 설정 미비로 인한 문제.

### Try (개선해야 할 것)
- **Retrobot 권한 안정화**: `allowedTools` 및 `dangerously-skip-permissions` 설정을 통합 검토하여 단일 실행으로 완료되도록 수정. `.githooks/post-commit`에서 권한 에러 발생 시 재시도 로직 제거 또는 최대 1회로 제한.
- **개인 문서 gitignore 추가**: `ver*.md`, `*_approach.md`, `*_claude.md` 등 개인 작업 파일 패턴을 `.gitignore`에 추가하여 실수 커밋 방지.
- **Retrobot SKILL.md 명확화**: "KPT 회고 자동 생성" 임을 첫 줄에 강조하고, 코드 리뷰와의 차이점을 명시. 사용자가 혼동하지 않도록 README.md에도 간략 설명 추가.
- **Step별 커밋 전략 구체화**: 각 Step 완료 시점에 명확한 커밋 메시지 규칙을 사전 합의하여 히스토리 가독성 향상. <!-- 직접 추가하세요 -->

## 타임라인

| 시각 (KST) | 세션 | 주요 활동 |
|------------|------|----------|
| 02:31 | 77adfa6c | 주 개발 세션 시작 — 커밋 메시지 추천, 플랜 리뷰 |
| 02:33 | f46a4f15 | Retrobot 자동 실행 #1 |
| 02:51 | ad03e402 | Retrobot 자동 실행 #2 |
| ~03:00 | 77adfa6c | BM25 하이브리드 검색 설계, 코드 구현 |
| ~05:00 | 77adfa6c | 프로젝트 흐름 설명, 개인 파일 처리 |
| ~07:00 | 77adfa6c | 에이전트 모드 구현 실행, 커밋 |
| 08:22 | 7297ab38 | Retrobot 자동 실행 #3 |
| 08:26 | db4c7a9f | Retrobot 자동 실행 #4 |
| 08:27 | 687c95b9 | Retrobot 자동 실행 #5 |
| 08:46 | 191cc066 | Retrobot 자동 실행 #6 |
| 08:56 | 77adfa6c | 주 개발 세션 종료 — Retrobot 목적 확인 |
| 08:56 | d58ff82e | Retrobot 자동 실행 #7 |
