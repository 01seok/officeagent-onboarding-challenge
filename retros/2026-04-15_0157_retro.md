# Retrobot 회고 — 2026-04-15 01:57 KST

## 요약

| 항목 | 값 |
|------|-----|
| 분석 기간 | 2026-04-15 01:38 ~ 01:57 KST |
| 커밋 수 | 1 |
| 주요 변경 파일 | `app/infra/cache.py` |
| 에러 발생 | 0 (기록된 에러 없음) |

## 커밋별 활동

### `44c9a62` — [BE] fix: 캐시 역인덱스 누락 및 인덱스 Set TTL 추가
- `doc_id=None` 전체 문서 대상 질문 캐시를 `doc_cache_index:__all__` 키로 역인덱스 등록
- `invalidate()` 에서 doc_id 인덱스 + `__all__` 인덱스 함께 무효화하도록 수정
- 인덱스 Set 자체에 TTL(72h) 추가. 만료된 캐시 키가 Set 멤버로 무한 누적되는 문제 해결

## KPT 회고

### Keep (좋았던 것)

- **Codex Adversarial Review로 설계 검증**: Claude로 구현한 코드를 Codex로 적대적 리뷰하는 흐름을 썼다. 단순 결함 탐지가 아닌 설계 전제와 트레이드오프를 질문하는 방식이라 놓쳤던 부분이 명확하게 드러났다.

- **리뷰 결과를 맥락 없이 따르지 않고 판단해서 취사선택**: Codex가 레이스 컨디션을 high로 지적했지만, 이 과제의 성격(단일 프로세스 asyncio, 소규모 데모)을 고려해 버전 키 구현은 하지 않기로 결정했다. 복잡도를 높이는 것보다 트레이드오프를 인지하고 문서화하는 쪽을 선택했다. 실제 레이스 가능성은 있지만 이 규모에서 발생할 트래픽 환경이 아니라는 판단이었다.

- **인덱스 Set TTL은 간단하고 의미 있는 개선**: `sadd` + `expire`를 파이프라인으로 묶어 원자적으로 처리했다. 한 줄짜리 수정이지만 Set이 무한 증가하는 문제를 실질적으로 막는다.

### Problem (나빴던 것)

- **doc_id=None 캐시를 역인덱스에서 빠뜨린 것**: 처음 역인덱스 설계 시 `if doc_id:` 조건을 걸어버렸다. 전체 문서 대상 질문도 캐싱되는데 그 무효화 경로를 처음부터 고려하지 못했다.

### Try (개선해야 할 것)

- **레이스 컨디션은 ARCHITECTURE.md에 문서화**: 버전 키 방식으로의 개선 방향을 설계 문서에 남겨둔다. 면접에서 "알고 있었냐"는 질문에 대한 답이 된다.

## 타임라인

| 시각 (KST) | 커밋 | 주요 활동 |
|------------|------|----------|
| 2026-04-15 01:38~ | | Codex Adversarial Review 실행 및 결과 분석 |
| 2026-04-15 01:57 | `44c9a62` | 캐시 역인덱스 누락 수정, 인덱스 Set TTL 추가 |
